<%= form_for(video_request_comment) do |f| %>
  <% if video_request_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_request_comment.errors.count, "error") %> prohibited this video_request_comment from being saved:</h2>

      <ul>
      <% video_request_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
